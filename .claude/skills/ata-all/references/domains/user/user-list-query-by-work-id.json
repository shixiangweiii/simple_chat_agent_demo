{
  "version": 1,
  "id": "ata::user-list-query-by-work-id",
  "description": "根据工号查询该用户对应的 ATA 用户 id。ATA 用户id一般是11或12开头的11位数字。工号为字符串，可含字母或数字，多数为数字。ATA 用户 id 用于需要 `userId` 的入参。",
  "javaMethod": "com.alibaba.atasuper.client.v2.user.DomainUserClientService#listQueryByWorkId",
  "inputSchema": {
    "description": "根据工号查询该用户对应的 ATA 用户 id。ATA 用户id一般是11或12开头的11位数字。工号为字符串，可含字母或数字，多数为数字。ATA 用户 id 用于需要 `userId` 的入参。",
    "properties": {
      "fieldName_0": {
        "description": "查询的工号列表",
        "items": {
          "description": "查询的工号列表",
          "type": "string"
        },
        "type": "array"
      },
      "fieldName_1": {
        "description": "返回字段的选择器，可以返回一些额外字段，在选择器中没有的字段默认这个接口都会直接返回的，在用户没有特别说明的情况下选择器建议不传任何字段",
        "type": "object"
      }
    },
    "required": [
      "fieldName_0"
    ],
    "type": "object"
  },
  "outputSchema": {
    "description": "AtaListResult<DomainUserClientDTO> 用户列表结果",
    "type": "object",
    "properties": {
      "success": {
        "type": "boolean",
        "description": "是否成功"
      },
      "errorCode": {
        "type": "string",
        "description": "错误编码"
      },
      "errorMsg": {
        "type": "string",
        "description": "错误信息"
      },
      "traceId": {
        "type": "string",
        "description": "追踪id"
      },
      "content": {
        "type": "array",
        "description": "用户列表",
        "items": {
          "$ref": "#/$defs/DomainUserClientDTO"
        }
      }
    },
    "$defs": {
      "DomainUserClientDTO": {
        "type": "object",
        "description": "用户信息",
        "properties": {
          "id": {
            "type": "number",
            "description": "用户id"
          },
          "workId": {
            "type": "string",
            "description": "用户工号"
          },
          "realname": {
            "type": "string",
            "description": "真实姓名"
          },
          "nickname": {
            "type": "string",
            "description": "花名"
          },
          "deptNo": {
            "type": "string",
            "description": "员工部门编号"
          },
          "showDepartment": {
            "type": "string",
            "description": "部门名称，用于前端展示"
          }
        },
        "required": [
          "id",
          "workId",
          "realname",
          "nickname",
          "deptNo"
        ]
      }
    }
  }
}
